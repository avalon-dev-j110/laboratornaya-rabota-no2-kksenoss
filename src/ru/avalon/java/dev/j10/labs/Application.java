package ru.avalon.java.dev.j10.labs;

import ru.avalon.java.dev.j10.labs.shapes.Shape;

public class Application {
/*
    public static void main(String[] args) {
        Shape[] shapes = new Shape[20];
        shapes[0] =  new Cercle();
        shapes[1] =  new Elipse();
        shapes[2] =  new Polygon();
        shapes[3] =  new Rectangle();
        shapes[4] =  new Triangle();
        shapes[5] =  new Elipse();
        shapes[6] =  new Polygon();
        shapes[7] =  new Rectangle();
        shapes[8] =  new Triangle();
        shapes[9] =  new Elipse();
        
        shapes[0] = new Triangle(new PointOfShape(x1,y1), new PointOfShape(x2,y2), new PointOfShape(x3,y3));
        System.out.println("Треугольник " + shapes[0].toString());
        
    Сортировка массива
Сортировка (упорядочение по значениям) массива a производится методами Arrays.sort(a) и Arrays.sort(a, index1, index2). Первый метод упорядочивает в порядке возрастания весь массив, второй — часть элементов (от индекса index1 до индекса index2). Имеются и более сложные методы сортировки. Элементы массива должны быть сравниваемы (поддерживать операцию сравнения).
        
    упоряд в порядке возрастания , вывести послед знач
    
    
    
    
    /*
         * TODO: Выполнить действия над массивом 'shapes'
         *
         * 1. Проинициализировать переменную 'shapes' массивом
         *    содержащим 10 произвольных фигур. Массив должен
         *    содержать экземпляры классов Circle, Rectangle
         *    и Triangle.
         *
         * 2. Найти в массиве 'shapes' фигуру с максимальной
         *    площадью. Для поиска фигуры необходимо создать
         *    статический метод в текущем классе (Application).
         */
    }


